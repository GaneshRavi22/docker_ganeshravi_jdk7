dockerfiles-centos-jdk7
========================

CentOS 7 dockerfile for Oracle JDK7

Get the version of Docker

    # docker version

To build:

Copy the sources down -

    # docker build -rm -t <USERNAME>/jdk7:centos7 .

To run:

    # docker run <USERNAME>/jdk7:centos7
